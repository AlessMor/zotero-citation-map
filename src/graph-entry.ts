/**
 * Citation Map is rendered directly by the parent-process plugin code.
 *
 * This entry point remains only because zotero-plugin.config.ts still bundles
 * it. graph.xhtml intentionally does not load the generated script.
 */
export {};
